import { Operator } from "./operator";

export type RocketSmokeAnimationParameters = {
    timingMs: number;
    distance: number;
    offset: number;
    operatorX: Operator;
    operatorY: Operator;
}